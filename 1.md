/* STREAMING_CHUNK:Defining CSS variables and global reset... */
/* 기본 스타일 및 변수 설정 */
:root {
    --bg-color: #faf6f0;            /* 따뜻한 베이지 배경 */
    --card-bg: #ffffff;             /* 흰색 카드 배경 */
    --primary-color: #6b4226;        /* 브라운 테마 컬러 */
    --primary-hover: #54331c;        /* 버튼 호버 브라운 */
    --text-color: #333333;           /* 기본 글자 색상 */
    --text-muted: #666666;          /* 부제목 글자 색상 */
    --border-color: #e2d7cd;        /* 테두리 색상 */
    --focus-shadow: rgba(107, 66, 38, 0.2); /* 포커스 아우트라인 그림자 */
    --success-bg: #e8f5e9;           /* 성공 메시지 연두색 배경 */
    --success-text: #2e7d32;         /* 성공 메시지 초록색 글자 */
}

* {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
}

body {
    font-family: '맑은 고딕', 'Malgun Gothic', -apple-system, sans-serif;
    background-color: var(--bg-color);
    color: var(--text-color);
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    padding: 20px 15px;
}

/* STREAMING_CHUNK:Styling main container and header... */
/* 주문서 컨테이너 설정 */
.order-container {
    width: 100%;
    max-width: 520px; /* 요구사항: 최대 너비 520px, 가운데 정렬 */
    background-color: var(--card-bg);
    border-radius: 16px;
    padding: 28px 24px;
    box-shadow: 0 10px 25px rgba(107, 66, 38, 0.08);
}

/* 상단 카페 로고 및 헤더 */
.cafe-header {
    text-align: center;
    margin-bottom: 24px;
    padding-bottom: 16px;
    border-b: 1px solid var(--border-color);
}

.logo {
    font-size: 48px;
    margin-bottom: 8px;
}

.cafe-title {
    font-size: 26px;
    color: var(--primary-color);
    font-weight: bold;
    margin-bottom: 4px;
}

.cafe-subtitle {
    font-size: 14px;
    color: var(--text-muted);
}

/* STREAMING_CHUNK:Styling form elements and options... */
/* 폼 그룹 및 입력 요소 스타일 */
.form-group {
    margin-bottom: 18px;
}

.form-label {
    display: block;
    font-size: 14px;
    font-weight: bold;
    margin-bottom: 6px;
    color: var(--primary-color);
}

.required {
    color: #d32f2f;
}

/* 입력칸 공통 스타일 (인풋, 셀렉트, 텍스트에어리어) */
.form-input,
.form-select,
.form-textarea {
    width: 100%;
    padding: 10px;
    font-size: 14px;
    border: 1px solid var(--border-color);
    border-radius: 8px;
    background-color: #fff;
    font-family: inherit;
    transition: all 0.2s ease-in-out;
}

/* 포커스 효과 */
.form-input:focus,
.form-select:focus,
.form-textarea:focus {
    outline: none;
    border-color: var(--primary-color);
    box-shadow: 0 0 0 3px var(--focus-shadow);
}

/* 가로 배치 옵션 그룹 (라디오 & 체크박스) */
.option-group.horizontal {
    display: flex;
    gap: 12px;
    align-items: center;
    padding-top: 4px;
}

.option-group.flex-wrap {
    flex-wrap: wrap;
}

.radio-label,
.checkbox-label {
    display: flex;
    align-items: center;
    gap: 6px;
    font-size: 14px;
    cursor: pointer;
    user-select: none;
}

.radio-label input,
.checkbox-label input {
    accent-color: var(--primary-color);
    width: 16px;
    height: 16px;
    cursor: pointer;
}

/* STREAMING_CHUNK:Styling price display and action buttons... */
/* 실시간 예상 금액 표시 영역 */
.price-container {
    text-align: center;
    font-size: 24px;
    font-weight: bold;
    color: var(--primary-color);
    margin: 24px 0 16px 0;
    padding: 12px;
    background-color: var(--bg-color);
    border-radius: 8px;
}

/* 버튼 그룹 및 개별 버튼 스타일 */
.button-group {
    display: flex;
    gap: 10px;
}

.btn {
    flex: 1;
    padding: 12px;
    font-size: 15px;
    font-weight: bold;
    border: none;
    border-radius: 8px;
    cursor: pointer;
    transition: background-color 0.2s;
    font-family: inherit;
}

/* 주문하기 버튼 */
.btn-primary {
    background-color: var(--primary-color);
    color: #ffffff;
}

.btn-primary:hover {
    background-color: var(--primary-hover);
}

/* 다시 작성 버튼 */
.btn-secondary {
    background-color: #e0e0e0;
    color: #333333;
}

.btn-secondary:hover {
    background-color: #d0d0d0;
}

/* STREAMING_CHUNK:Styling confirm message box... */
/* 주문 확인 안내 메시지 박스 */
.confirm-message {
    margin-top: 20px;
    padding: 14px 16px;
    background-color: var(--success-bg);
    color: var(--success-text);
    border-radius: 8px;
    font-size: 14px;
    font-weight: bold;
    line-height: 1.5;
    text-align: center;
    border: 1px solid #c8e6c9;
}

.hidden {
    display: none;
}